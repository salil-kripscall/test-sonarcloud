<%= form_with model: @vendor do |form| %>
  <div class="form-group">
    <%= form.label :name %><br>
    <%= form.text_field :name, class: "form-control" %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>